def action_get_text(self, by, locator, value=None, descricao=None, resultados=None, pin=None):
    """
    Captura texto de um elemento, considerando o estado do colaborador (CLOTURE ou ACTIF).
    - Se o estado for 'CLOTURE', captura normalmente (a data estará visível).
    - Se o estado for 'ACTIF', ignora o passo.
    - Caso contrário, tenta capturar o texto como fallback genérico.
    """
    try:
        elem = self.wait.until(EC.presence_of_element_located((by, locator)))
        estado_elem = self.wait.until(EC.presence_of_element_located((By.ID, "etatPo")))

        estado = estado_elem.text.strip().upper()
        texto_elem = elem.text.strip() if elem.text else ""

        # 🔹 Caso 1: Estado = CLOTURE → captura data (ex: 15/09/2023)
        if estado == "CLOTURE" and "/" in texto_elem:
            if resultados is not None and descricao:
                resultados[descricao] = texto_elem
                self.log(f"✅ Capturado '{descricao}' (CLOTURE): {texto_elem}", "DEBUG")

        # 🔹 Caso 2: Estado = ACTIF → pula passo
        elif estado == "ACTIF":
            self.log(f"⏭️ Pulando passo '{descricao}' (colaborador ativo)", "INFO")
            return

        # 🔹 Caso 3: Outro estado → tenta capturar se tiver conteúdo
        else:
            if resultados is not None and descricao:
                resultados[descricao] = texto_elem
                self.log(f"ℹ️ Capturado '{descricao}' (outro estado '{estado}'): {texto_elem}", "DEBUG")

    except Exception as e:
        self.log(f"⚠️ Erro ao capturar '{descricao}': {e}", "WARNING")
        pass
