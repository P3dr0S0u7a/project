@staticmethod
def action_transform_parse_dates(df, config):
    """Converte colunas para datetime, com tolerância a múltiplos formatos"""
    columns = config.get("columns", [])
    columns = [columns] if isinstance(columns, str) else columns

    def parse_datas(x):
        """Transforma strings em datas, aceitando diferentes formatos"""
        if pd.isnull(x):
            return x
        try:
            return pd.to_datetime(x, format="%d/%m/%Y %H:%M:%S", dayfirst=True, errors="coerce")
        except Exception:
            try:
                return pd.to_datetime(x, dayfirst=True, errors="coerce")
            except Exception:
                return x

    for col in columns:
        if col not in df.columns:
            WebScrapping.log(f"⚠️ Coluna '{col}' não encontrada no DataFrame.", "WARNING")
            continue

        try:
            # Aplica o parser linha a linha com tolerância a erros
            df[col] = df[col].apply(parse_datas)
            WebScrapping.log(f"📅 Coluna '{col}' convertida para datetime", "INFO")
        except Exception as e:
            WebScrapping.log(f"❌ Erro ao converter coluna '{col}' para datetime: {e}", "ERROR")

    return df
